
// import { commonGet } from '../../api/common';

// function getTime(dateStr) {
//   const date = new Date(dateStr);
//   const Month = date.getMonth() + 1;
//   const Day = date.getDate();
//   const minute = date.getHours();
//   const f = date.getMinutes();
//   const s = date.getSeconds();
//   const Y = `${date.getFullYear()}-`;
//   const M = Month < 10 ? `0${Month}-` : `${Month}-`;
//   const D = Day + 1 < 10 ? `0${Day} ` : `${Day} `;
//   const m = minute + 1 < 10 ? `0${minute}:` : `${minute}:`;
//   const S = s + 1 < 10 ? `0${s}` : `${s}`;
//   return Y + M + D + m + f;
// }

const state = {
};
const getters = {
};
const actions = {
};
const mutations = {
};
export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
